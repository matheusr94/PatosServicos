<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="matheusrodrigues.androidapps.patosservicos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PatosServicos">
        <activity android:name=".activity.DetalhesServicosActivity" android:parentActivityName=".activity.AnunciosActivity"></activity>
        <activity android:name=".activity.CadastrarServicosActivity" />
        <activity
            android:name=".activity.MeusServicosActivity"
            android:parentActivityName=".activity.AnunciosActivity" />
        <activity android:name=".activity.AnunciosActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.CadastroActivity" />
        <activity android:name=".activity.AutenticacaoActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>