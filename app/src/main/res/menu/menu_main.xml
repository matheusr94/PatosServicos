<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:id="@+id/group_logados" android:visible="false">
        <item android:id="@+id/menu_anuncios"
            android:title="Meus anúncios"/>
        <item android:id="@+id/menu_sair"
            android:title="Sair"/>
    </group>

    <group android:id="@+id/group_deslogados" android:visible="false">
        <item android:id="@+id/menu_autenticar"
            android:title="Cadastrar / Entrar"
            app:showAsAction="always"/>
    </group>

</menu>